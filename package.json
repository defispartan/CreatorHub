{
  "name": "creatorhub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install": "cd ./client && bun install",
    "build": "cd ./client && bun run build",
    "dev": "cd ./client && bun run dev && ./pocketbase",
    "dev:static": "bun run build && ./pocketbase serve"
  }
}
